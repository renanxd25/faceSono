import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SemconexaocomservidorPage } from './semconexaocomservidor.page';

describe('SemconexaocomservidorPage', () => {
  let component: SemconexaocomservidorPage;
  let fixture: ComponentFixture<SemconexaocomservidorPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SemconexaocomservidorPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
